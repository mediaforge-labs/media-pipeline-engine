name: DISABLED - Dell Canary Resume

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Use MediaForge Production Factory recovery.'
