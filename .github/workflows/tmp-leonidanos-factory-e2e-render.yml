name: DISABLED - Temporary Leonidanos E2E Render

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Canonical production is MediaForge Production Factory.'
