name: DISABLED - Legacy Production Test Runner

on:
  workflow_dispatch:

# Disabled to prevent a second executable production path.
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Use MediaForge Production Factory plus contract checks.'
