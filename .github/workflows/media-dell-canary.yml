name: DISABLED - Legacy Dell Canary

on:
  workflow_dispatch:

# Disabled. Dell supplies media only; all production runs through MediaForge Production Factory.
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by canonical architecture.'
