name: DISABLED - Legacy Media Pipeline

on:
  workflow_dispatch:

# Disabled to enforce a single production factory.
# Canonical workflow: .github/workflows/media-factory.yml
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Use MediaForge Production Factory.'
