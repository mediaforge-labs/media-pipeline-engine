name: DISABLED - Dell Integration Smoke

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Supplier validation is covered by the repository contract check.'
