name: DISABLED - Legacy Music Postprocess

on:
  workflow_dispatch:

# Disabled. Production remains music-disabled and fully inside MediaForge Production Factory.
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by canonical architecture.'
