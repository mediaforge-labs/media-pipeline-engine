name: DISABLED - Legacy Calibration Trigger

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled to keep a single production architecture.'
