name: DISABLED - Legacy Canary Watermark Repair

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by canonical architecture.'
